# CodeAid Project - সহজ গাইড (Easy Guide)

ভাইয়া/আপু, আপনি বলেছেন আপনি কোড খুব বেশি বোঝেন না। চিন্তার কোনো কারণ নেই! এই ফাইলে আমি একদম সহজ ভাষায় সব কিছু বুঝিয়ে বলছি। ধাপে ধাপে কাজগুলো করলে প্রজেক্টটি খুব সহজেই রান করতে পারবেন।

---

## ১. রিসার্চ পেপারটি আসলে কী নিয়ে? (What is the paper about?)
রিসার্চ পেপারটির নাম **"CodeAid"**। সাধারণ ChatGPT-কে যদি বলা হয় "আমাকে এই কোডটি লিখে দাও", সে পুরো কোড লিখে দেয়। এতে ছাত্রছাত্রীরা কোড কপি করে, কিন্তু কিছু শেখে না। 

CodeAid পেপারটিতে এমন একটি AI অ্যাসিস্ট্যান্ট বানানো হয়েছে, যা ছাত্রদের **সরাসরি কোড লিখে দেবে না**। বরং এটি একজন শিক্ষকের মতো কাজ করবে:
- কোড কোথায় ভুল হয়েছে তার ক্লু বা ধারণা দেবে।
- কনসেপ্ট বুঝিয়ে বলবে।
- অ্যালগরিদম বলে দেবে, কিন্তু মূল কোড নিজেকেই লিখতে হবে।

**আমাদের প্রজেক্টের কাজ:** আমরা ঠিক এরকমই একটা ওয়েবসাইট বানিয়েছি (CodeAid), যেখানে ৫টি মূল ফিচার আছে।

---

## ২. API Key ও JWT_SECRET এর কাজ কী? (What are they?)

**JWT_SECRET:** 
`.env.example` ফাইলে দেখবেন `JWT_SECRET=super_secret_key` লেখা আছে। এটি হলো একটি পাসওয়ার্ড যা দিয়ে ইউজারদের লগইন সেশন সুরক্ষিত রাখা হয়। আপনার এটি নিয়ে কিছুই করতে হবে না, যেমন আছে তেমনই রেখে দিন।

**OPENAI_API_KEY:**
আপনার যদি কোনো কার্ড বা API Key না থাকে, তাহলেও কোনো সমস্যা নেই! আমি প্রজেক্টটিতে একটি **"Mock Mode" (ডেমো মোড)** তৈরি করে দিয়েছি। 
অর্থাৎ, আপনি যদি API Key না দেন, তবুও প্রজেক্টটি ক্র্যাশ করবে না। এটি ডেমো হিসেবে কাজ করবে এবং আপনাকে ডামি রেসপন্স দেখাবে, যা প্রেজেন্টেশন বা টেস্টিং এর জন্য যথেষ্ট!

আর যদি API Key দিতে চান, তাহলে:
1. [platform.openai.com](https://platform.openai.com/) থেকে একটি "Create new secret key" করে কপি করুন।
2. আপনার প্রজেক্টের `.env.example` ফাইলটির নাম পালটে `.env` রাখুন।
3. সেখানে `OPENAI_API_KEY=your_openai_api_key_here` এর জায়গায় আপনার Key টি বসিয়ে দিন।

---

## ৩. প্রজেক্ট সেটআপ (.env ফাইল তৈরি করা)
প্রজেক্টটি কাজ করানোর জন্য `.env.example` ফাইলটিকে `.env` ফাইলে রূপান্তর করতে হবে।

1. আপনার প্রজেক্টের মেইন ফোল্ডারে (Desktop/ML Lab Project) দেখুন `.env.example` নামে একটি ফাইল আছে।
2. ফাইলটির নাম পালটে (Rename) শুধু `.env` রাখুন। 
3. এবার ফাইলটি ওপেন করুন। ভেতরে দেখবেন লেখা আছে: `OPENAI_API_KEY=your_openai_api_key_here`
4. `your_openai_api_key_here` লেখাটি মুছে সেখানে আপনার কপি করা API Key বসিয়ে দিন।
   - যেমন: `OPENAI_API_KEY=sk-proj-xyz123...`
5. ফাইলটি Save করুন।

---

## ৪. প্রজেক্ট কীভাবে রান করবেন? (How to run)

প্রজেক্টের তিনটি অংশ আছে। আমরা মূলত Frontend (ওয়েবসাইট) এবং Backend (সার্ভার) রান করবো।

**ধাপ ১: Backend (সার্ভার) রান করা**
1. VS Code বা টার্মিনালে নতুন একটি উইন্ডো খুলুন।
2. `backend` ফোল্ডারে যান: 
   ```bash
   cd backend
   ```
3. এবার রান করার কমান্ড দিন:
   ```bash
   npx tsc
   node dist/server.js
   ```
   (যদি `Server running on port 5000` লেখা আসে, তার মানে সার্ভার ঠিকঠাক চলছে।)

**ধাপ ২: Frontend (ওয়েবসাইট) রান করা**
1. টার্মিনালে আরও একটি নতুন উইন্ডো খুলুন।
2. `frontend` ফোল্ডারে যান:
   ```bash
   cd frontend
   ```
3. এবার রান করুন:
   ```bash
   npm run dev
   ```
4. টার্মিনালে একটি লিংক দেখতে পাবেন (যেমন: `http://localhost:5173/`)। লিংকটিতে `Ctrl` চেপে ক্লিক করুন। আপনার ব্রাউজারে CodeAid ওয়েবসাইটটি ওপেন হয়ে যাবে!

---

## ৫. কীভাবে টেস্ট করবেন? (How to test)

ব্রাউজারে ওয়েবসাইট ওপেন হলে দেখবেন লগইন করার অপশন আছে। যেকোনো ইউজারনেম এবং পাসওয়ার্ড দিয়ে "Register" করুন, এরপর ড্যাশবোর্ড চলে আসবে।

ড্যাশবোর্ডের বাম পাশে কোড লেখার জায়গা, আর ডান পাশে চ্যাট করার জায়গা। 

**টেস্ট করার নিয়ম:**
1. **General Question:** ড্রপডাউন মেনু থেকে "General Question" সিলেক্ট করুন। চ্যাটবক্সে লিখুন: *"What is a pointer in C?"* এবং Submit করুন। দেখবেন AI আপনাকে সহজ করে পয়েন্টার বুঝিয়ে দিচ্ছে, কিন্তু কোনো বড় কোড দিচ্ছে না।
2. **Help Write Code:** "Help Write Code" সিলেক্ট করুন। লিখুন: *"Write a C program to find the max number in an array"*. দেখবেন AI আপনাকে সরাসরি কোড না দিয়ে, ধাপে ধাপে অ্যালগরিদম ও সিউডোকোড (Pseudocode) বলে দিচ্ছে।
3. **Explain Code:** বাম পাশের কোড এডিটরে ছোট একটি C কোড লিখুন। এরপর "Explain Code" সিলেক্ট করে Submit করুন। দেখবেন AI কোডের প্রতিটি লাইন ব্যাখ্যা করে দিচ্ছে।

এভাবেই আপনার প্রজেক্টটি কাজ করবে এবং আপনি আপনার স্যার/ম্যামকে প্রেজেন্টেশনের সময় ঠিক এগুলোই দেখাতে পারবেন!
